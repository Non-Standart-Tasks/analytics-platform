{
  "title": "Project Overview (any service)",
  "uid": "project-overview",
  "tags": ["multi-service", "telemetry"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "30s",
  "time": {"from": "now-24h", "to": "now"},
  "templating": {
    "list": [
      {
        "name": "service",
        "label": "Project",
        "type": "query",
        "datasource": {"type": "postgres", "uid": "analytics-postgres"},
        "query": "SELECT DISTINCT service FROM events ORDER BY service;",
        "refresh": 1,
        "sort": 1,
        "multi": true,
        "includeAll": true,
        "current": {"selected": false, "text": "All", "value": "$__all"}
      }
    ]
  },
  "panels": [
    {
      "id": 1, "type": "stat", "title": "Поисков за период",
      "description": "Количество завершённых пользовательских поисков (events.type = 'scenario.completed').",
      "gridPos": {"x": 0, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT count(*) AS value FROM events WHERE service IN ($service) AND type='scenario.completed' AND $__timeFilter(received_at);"}]
    },
    {
      "id": 2, "type": "stat", "title": "Конверсия (выдан ≥1 билет)",
      "description": "% поисков, где юзеру показали хотя бы один билет: tickets_shown > 0.",
      "gridPos": {"x": 4, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "fieldConfig": {"defaults": {"unit": "percent",
        "thresholds": {"mode": "absolute", "steps": [
          {"color": "red", "value": null}, {"color": "orange", "value": 70}, {"color": "green", "value": 90}]}}},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT (count(*) FILTER (WHERE (payload->'scenario'->>'tickets_shown')::int > 0) * 100.0 / NULLIF(count(*),0))::numeric(5,1) AS value FROM events WHERE service IN ($service) AND type='scenario.completed' AND $__timeFilter(received_at);"}]
    },
    {
      "id": 3, "type": "stat", "title": "Уникальных юзеров",
      "gridPos": {"x": 8, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT count(DISTINCT payload->'scenario'->>'user_email') AS value FROM events WHERE service IN ($service) AND payload->'scenario'->>'user_email' IS NOT NULL AND $__timeFilter(received_at);"}]
    },
    {
      "id": 4, "type": "stat", "title": "Avg билетов на поиск",
      "gridPos": {"x": 12, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "fieldConfig": {"defaults": {"decimals": 1}},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT avg((payload->'scenario'->>'tickets_shown')::int)::numeric(10,1) AS value FROM events WHERE service IN ($service) AND type='scenario.completed' AND payload->'scenario'->>'tickets_shown' IS NOT NULL AND $__timeFilter(received_at);"}]
    },
    {
      "id": 5, "type": "stat", "title": "Pipeline p95 (sec)",
      "description": "95-й перцентиль длительности всего поиска.",
      "gridPos": {"x": 16, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "fieldConfig": {"defaults": {"unit": "s",
        "thresholds": {"mode": "absolute", "steps": [
          {"color": "green", "value": null}, {"color": "orange", "value": 20}, {"color": "red", "value": 35}]}}},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT (percentile_cont(0.95) WITHIN GROUP (ORDER BY (payload->'scenario'->'timings_ms'->>'total')::int) / 1000.0)::numeric(10,1) AS value FROM events WHERE service IN ($service) AND type='scenario.completed' AND payload->'scenario'->'timings_ms'->>'total' IS NOT NULL AND $__timeFilter(received_at);"}]
    },
    {
      "id": 6, "type": "stat", "title": "LLM-вызовов",
      "gridPos": {"x": 20, "y": 0, "w": 4, "h": 4},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT count(*) AS value FROM events WHERE service IN ($service) AND type='llm.inference.completed' AND $__timeFilter(received_at);"}]
    },
    {
      "id": 7, "type": "timeseries", "title": "События по времени (по type, stacked)",
      "gridPos": {"x": 0, "y": 4, "w": 16, "h": 8},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "fieldConfig": {"defaults": {"custom": {"drawStyle": "bars", "stacking": {"mode": "normal"}}}},
      "targets": [{"refId": "A", "format": "time_series",
        "rawSql": "SELECT $__timeGroup(received_at, '5m') AS time, type AS metric, count(*)::float AS value FROM events WHERE service IN ($service) AND $__timeFilter(received_at) GROUP BY 1, 2 ORDER BY 1;"}]
    },
    {
      "id": 8, "type": "bargauge", "title": "Топ event types",
      "gridPos": {"x": 16, "y": 4, "w": 8, "h": 8},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "options": {
        "orientation": "horizontal",
        "displayMode": "gradient",
        "showUnfilled": true,
        "valueMode": "color",
        "reduceOptions": {"values": true, "calcs": ["lastNotNull"], "fields": "/^events$/"}
      },
      "fieldConfig": {"defaults": {"unit": "short", "color": {"mode": "continuous-BlPu"}}},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT type, count(*)::int AS events FROM events WHERE service IN ($service) AND $__timeFilter(received_at) GROUP BY 1 ORDER BY 2 DESC LIMIT 15;"}]
    },
    {
      "id": 9, "type": "table", "title": "Breakdown: service × type × status",
      "gridPos": {"x": 0, "y": 12, "w": 12, "h": 9},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT service AS \"Service\", type AS \"Type\", status AS \"Status\", count(*) AS \"Count\" FROM events WHERE service IN ($service) AND $__timeFilter(received_at) GROUP BY 1, 2, 3 ORDER BY 4 DESC LIMIT 50;"}]
    },
    {
      "id": 10, "type": "table", "title": "Последние события",
      "gridPos": {"x": 12, "y": 12, "w": 12, "h": 9},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT received_at AS \"Время\", service AS \"Service\", type AS \"Type\", status AS \"Status\", data AS \"Data\" FROM events WHERE service IN ($service) AND $__timeFilter(received_at) ORDER BY received_at DESC LIMIT 100;"}]
    },
    {
      "id": 11, "type": "table", "title": "Failed events (детали)",
      "gridPos": {"x": 0, "y": 21, "w": 24, "h": 8},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT received_at AS \"Время\", service AS \"Service\", type AS \"Type\", data AS \"Data\", payload::text AS \"Payload\" FROM events WHERE service IN ($service) AND status = 'failed' AND $__timeFilter(received_at) ORDER BY received_at DESC LIMIT 50;"}]
    },
    {
      "id": 12, "type": "table", "title": "Последние поиски (scenario.completed)",
      "gridPos": {"x": 0, "y": 29, "w": 24, "h": 10},
      "datasource": {"type": "postgres", "uid": "analytics-postgres"},
      "description": "Только для проектов, шлющих type='scenario.completed' с payload.scenario (avia-style schema).",
      "targets": [{"refId": "A", "format": "table",
        "rawSql": "SELECT received_at AS \"Время\", service AS \"Service\", payload->'scenario'->>'user_email' AS \"Email\", data AS \"Запрос\", (payload->'scenario'->>'tickets_shown')::int AS \"Билетов\", ((payload->'scenario'->'timings_ms'->>'total')::int / 1000.0)::numeric(10,1) AS \"Sec\", status AS \"Статус\", payload->'scenario'->>'aviasales_url' AS \"URL\" FROM events WHERE service IN ($service) AND type = 'scenario.completed' AND $__timeFilter(received_at) ORDER BY received_at DESC LIMIT 100;"}]
    }
  ]
}
